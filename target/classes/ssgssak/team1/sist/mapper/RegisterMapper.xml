<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ssgssak.team1.sist.mapper.RegisterMapper"> 
	<!-- <delete id="delete">
	DELETE FROM tbl_board
	WHERE bno=#{bno}
	</delete> -->	
  <insert id="register" parameterType="MemberVO">
  INSERT INTO member (id, email, phone_num, name, passwd, birth_d, register_date, login_notification, login2_notification, privilege, enabled)
        VALUES (#{id}, #{email}, #{phoneNum}, #{name}, #{passwd}, #{birthD}, #{registerDate}, #{loginNotification}, #{login2Notification}, #{privilege}, #{enabled}) 
  </insert>
</mapper>